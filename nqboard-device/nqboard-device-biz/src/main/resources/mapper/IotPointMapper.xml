<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mx.nqboard.device.mapper.IotPointMapper">
    <resultMap id="iotPointMap" type="com.mx.nqboard.device.api.vo.IotPointVO">
        <id property="id" column="id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="orderNum" column="order_num"/>
        <result property="delFlag" column="del_flag"/>
        <result property="protocolValue" column="protocol_value"/>
        <result property="deviceId" column="device_id"/>
        <result property="modelId" column="model_id"/>
        <association property="model" resultMap="com.mx.nqboard.device.mapper.IotModelMapper.iotModelMap"/>
    </resultMap>

    <sql id="iotPointSql">
        im.id,im.create_by,im.create_time,im.update_by,im.update_time,im.order_num,im.del_flag,im.name,im.identifier,im.type,im.data_type,im.base_value,im.proportional_coefficient,im.remark,im.specs,im.unit,
        ipt.protocol_value,ipt.device_id,ipt.model_id
    </sql>

    <select id="getPointDetailByDeviceId" resultMap="iotPointMap">
        SELECT
            <include refid="iotPointSql"/>
        FROM
            iot_model AS im
        INNER JOIN iot_point AS ipt ON ipt.model_id = im.id
        WHERE ipt.del_flag = '0' AND im.del_flag = '0' AND ipt.device_id = #{entity.deviceId}
        ORDER BY im.order_num
    </select>
</mapper>
